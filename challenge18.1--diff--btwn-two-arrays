function diffArray(arr1, arr2) {
	
	var combo = arr1.concat(arr2);
	console.log(combo);

	var result = combo.filter(function(num) {
		if((arr1.indexOf(num) === -1) || (arr2.indexOf(num) === -1)){
		return num;
	}});

	return result;

	}


diffArray([1, 2, 3], [4, 3, 6]);
